// src/context/tenant-context.js
import { createContext } from "react";

// Este contexto se utiliza para proporcionar el ID del inquilino (tenant) a toda la aplicación
export const TenantContext = createContext();
